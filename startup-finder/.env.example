# startup-finder 環境変数
#
# DATABASE_URL: 本番用Postgres（Supabase Transaction pooler推奨・ポート6543）。
#   未設定の場合はローカルSQLite（data/startup.db）で動く。
#   例: postgresql://postgres.<project-ref>:<password>@aws-1-<region>.pooler.supabase.com:6543/postgres
#   ※パスワードに記号を含む場合はURLエンコードすること（! → %21 など）
DATABASE_URL=
