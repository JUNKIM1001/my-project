1. ✅解消: `parseNumeric()` は `1e3`/`0x10`/`Infinity` を `NaN` にし、全角数字・全角小数点・全角マイナスを正規化、`app.js` も import 済み。`"+-"`/複数小数点/空白のみも拒否または `null`。

2. ✅解消: `practicalWarnings()` は目標割合 `>50%`、プール `>30%` で警告し、`app.js` は `computeRound()` の成否とは別に表示しているため計算停止しない。

3. ⚠️不完全: 境界値テストは概ね追加済みだが、持分合計 `99.94%` 側の拒否テストがなく、`parseNumeric` の先頭ゼロ・`"+-"`・全角混在の明示テストも不足。

## 新たな問題

[P2] `parseNumeric()` はカンマと空白を正規表現前に全除去するため、`"1 2"` や `"1,,000"`、`"12,34"` も通常表記として受理される。計算停止系ではないが、表記制限としてはやや緩い。
