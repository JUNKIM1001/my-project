項目1: `✅誤検知と確認`  
`exitAnalysis` 冒頭で `computeRound(input)`、`computeRound` 冒頭で `validateInput(input)` が実行される。`futureDilution`、`targetMultiple`、Exitマルチプル下限>上限は `validateInput` で弾かれ、`computeRound` が `{ok:false}`、`exitAnalysis` が `null` を返す。該当テストは実挙動と一致。

項目2: `✅解消`  
境界テストで `post == 2800` は `below`、`post == 5600` は `within` を明示検証している。

項目3: `✅解消`  
`targetMultiple<1`、`futureDilution<0`、Exitマルチプル0/負、`exitYears`・`exitMultipleLow/High` 欠落時の `null` がテストされている。

## 新たな問題
なし
