P1 `.claude/settings.local.json`:64-66  
`Bash(git add *)` / `Bash(git commit *)` / `Bash(git push *)` をローカル許可に追加しており、エージェントが明示確認なしに変更のステージング・コミット・push まで実行できる状態になります。コミット系は許可リストから外し、必要時だけ都度承認にしてください。

P1 `review-loop.sh`:63  
Claude 修正フェーズに `--allowedTools "Read,Edit,Bash"` を渡しており、直前コメントの「コミットや破壊的操作はさせない」と矛盾して任意 Bash を許可しています。`Bash` を外すか、必要な読み取り・テストコマンドだけに限定してください。

P2 `review-loop.sh`:23,41,47  
レビュー結果を `codex-reviews/` に保存していますが、このディレクトリが `.gitignore` にないため、次回以降 `git ls-files --others --exclude-standard` でレビュー対象に混入します。`codex-reviews/` を `.gitignore` に追加するか、ログ保存先をリポジトリ外/ignored 配下にしてください。

P2 `review-loop.sh`:50  
`grep -q "REVIEW_CLEAN"` は本文中に文字列が含まれただけで指摘なし扱いになります。仕様通り「最終行だけ」を判定するため、`tail -n 1 "$REVIEW_FILE" | grep -qx "REVIEW_CLEAN"` のように厳密化してください。
